Currently kubernetes is nowhere near functinal enough to use for the alpha so isntead we are opting to use this basic pipeline for mock grading

The goal is to temporarily serve the purpose of grading and getting back json outputs from the main.py file then pass it back to the springboot api

